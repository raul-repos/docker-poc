version: "3"
services:
  node-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - /app/node_modules #preventing removal of node_modules if it doesn't exist on local
    environment:
      - PORT=3000
